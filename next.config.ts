import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // i18n routing is handled manually via [locale] segment
  // No next-intl or next-i18next needed — lightweight custom hook
};

export default nextConfig;
